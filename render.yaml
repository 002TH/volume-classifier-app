services:
  - type: web
    name: solusdt-dashboard
    runtime: python
    pythonVersion: "3.9.7"
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.7